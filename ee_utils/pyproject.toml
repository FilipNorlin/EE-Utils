[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ee-utils"
version = "0.1.0"
description = "Electrical Engineering Utilities"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["ee_utils*"]